name: Run cargo test
inputs:
  path:
    required: true
env:
  RUSTFLAGS: -D warnings
runs:
  using: composite
  steps:
    - uses: actions/cache@v3
      with:
        path: |
          ${{ inputs.path }}/target/
        key: ${{ runner.os }}-cargo-test-${{ inputs.path }}-${{ hashFiles(format('{0}/Cargo.lock', inputs.path)) }}
        restore-keys: |
          ${{ runner.os }}-cargo-test-${{ inputs.path }}-
          ${{ runner.os }}-cargo-test-

    - name: Test
      shell: bash
      run: time cargo test  --manifest-path ${{ inputs.path }}/Cargo.toml

    - name: Did you push Cargo.lock
      shell: bash
      run: |
        if [[ $(git diff --name-only) ]]; then
            echo "::error::File change detected. If it's Cargo.lock, you should add it into git"
            echo  "::error::$(git diff --name-only)"
            exit 1
        else
            echo "Good. Cargo.lock is up to date (or it ignored by gitignore)"
        fi

    - name: Fmt
      shell: bash
      run: cargo fmt --manifest-path ${{ inputs.path }}/Cargo.toml

    - name: Did you forget run fmt
      shell: bash
      run: |
        if [[ $(git diff --name-only) ]]; then
            echo "::error::File change detected. Please run 'cargo fmt' before push"
            echo  "::error::$(git diff --name-only)"
            exit 1
        else
            echo "Good. Cargo.lock is up to date (or it ignored by gitignore)"
        fi

    - name: Did you forget run cargo clippy
      shell: bash
      run: |
        cargo clippy --manifest-path ${{ inputs.path }}/Cargo.toml -- -D warnings
        if [[ $? ]]; then
            echo "::error::Clippy failed. Please check code with 'cargo clippy' before push"
            exit 1
        else
            echo "Good. Clippy passed"
        fi
