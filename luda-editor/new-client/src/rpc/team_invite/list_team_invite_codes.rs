#![doc = r" This is Generated by rpc build.rs"]
use crate::{network::*, *};
pub use luda_rpc::team_invite::list_team_invite_codes::*;
pub type OptionResult = Option<Result<Response, Error>>;
pub fn list_team_invite_codes<'a, Deps: Dependencies + 'a>(
    ctx: &'a RenderCtx,
    request: impl FnOnce(Deps) -> Option<RefRequest<'a>>,
    dependencies: Deps,
) -> Sig<'a, OptionResult, &'a OptionResult> {
    server_rpc(ctx, request, dependencies, 7u16)
}
pub fn list_team_invite_codes_render<'a, Deps: Dependencies + 'a>(
    ctx: &'a RenderCtx,
    request: impl FnOnce(Deps) -> Option<RefRequest<'a>>,
    dependencies: Deps,
    on_loading: impl FnOnce(),
    on_err: impl FnOnce(&Error),
    on_res: impl FnOnce(&Response),
) {
    match list_team_invite_codes(ctx, request, dependencies).as_ref() {
        Some(result) => match result {
            Ok(res) => on_res(res),
            Err(err) => on_err(err),
        },
        None => on_loading(),
    }
}
impl ServerConnection {
    pub async fn list_team_invite_codes<'a>(
        &'a self,
        request: RefRequest<'a>,
    ) -> Result<Result<Response, Error>> {
        Ok(self
            .request(
                7u16,
                luda_rpc::rkyv::to_bytes::<_, 1024>(&request)
                    .unwrap()
                    .to_vec(),
            )
            .await?)
    }
}
